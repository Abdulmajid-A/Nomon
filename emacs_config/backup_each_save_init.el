;;bootstraping the packge (note for now we don't want it and also this package copies the whole file tree and has no limit on the number of files copied thus for no we shall not use it later when we put a limi we will)
;;(require 'backup-each-save)
;;(setq backup-each-save-mirror-location "~/.emacs.d/backup")
;;(add-hook 'after-save-hook 'backup-each-save)
	       
