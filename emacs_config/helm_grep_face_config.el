;;for the theme dark+ !!!!
(set-face-attribute 'helm-grep-lineno nil :foreground "#2c6fb3")

;;HAS NO EFFECT IF THE GREP BACKEND USES --color (which is the best practice to so as it's faster (:)
(set-face-attribute 'helm-grep-match nil :foreground "#FF00FF")
