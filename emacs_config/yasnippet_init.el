(use-package yasnippet
  :straight t
  :config
  (yas-global-mode 1)
  ;;for yasnippet per-buffer basis uncommnet the 2 lines below and comment the upper one
  ;;(yas-reload-all)
  ;;(add-hook 'prog-mode-hook #'yas-minor-mode);;move it the :hook part
  )
